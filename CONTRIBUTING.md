# Contributing to newsmast_mastodon

Thanks for your interest in improving `newsmast_mastodon`! This document
describes how to set up your environment, run the test suite, and submit
changes.

## Development setup

1. Clone the repository and `cd` into it.
2. Install dependencies and prepare the dummy app:

   ```bash
   bin/setup
   ```

   If you only want to prepare dependencies/database without starting a local
   dev server, run:

   ```bash
   bin/setup --skip-server
   ```

3. Run the test suite to confirm your environment works:

   ```bash
   bundle exec rspec
   ```

4. Run the linter:

   ```bash
   bundle exec rubocop
   ```

## Making changes

1. Create a feature branch from `main`:

   ```bash
   git checkout -b feat/my-change
   ```

2. Make focused commits with descriptive messages (see below).
3. Ensure `rspec` and `rubocop` pass locally.
4. Update `CHANGELOG.md` under the `Unreleased` section.
5. Open a pull request against `main` and fill in the PR template.

## Coding standards

- Follow the rules in `.rubocop.yml` (inherits from `rubocop-rails-omakase`).
- Keep public APIs namespaced under `NewsmastMastodon::`.
- Prefer small, composable service objects over large controllers/models.
- Add or update specs for every behavior change.

## Configuration reference

For runtime configuration variables, see
[`docs/configuration/environment-variables.md`](docs/configuration/environment-variables.md).

## Commit message conventions

We follow [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<optional scope>): <short summary>

<optional body>

<optional footer>
```

Common types: `feat`, `fix`, `refactor`, `perf`, `docs`, `test`, `chore`,
`build`, `ci`.

## Reporting bugs or requesting features

Please use the GitHub issue templates under `.github/ISSUE_TEMPLATE/`.
For security-sensitive issues, see [`SECURITY.md`](SECURITY.md) instead of
opening a public issue.

For support and community channels, see [`SUPPORT.md`](SUPPORT.md).

For maintainer roles and review ownership, see [`MAINTAINERS.md`](MAINTAINERS.md).

For decision and merge policy, see [`GOVERNANCE.md`](GOVERNANCE.md).

For expected conduct in community spaces, see [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md).

## Versioning and compatibility policy

This project follows Semantic Versioning.

- Major releases may include breaking changes.
- Minor releases add behavior in a backward-compatible way.
- Patch releases are for fixes and should avoid behavior changes.

Breaking changes must:

1. Be called out under `Unreleased` in `CHANGELOG.md`.
2. Include migration guidance in the pull request description.
3. Include tests that prove both the old behavior impact and the new behavior.

## Release checklist (maintainers)

1. Ensure all intended changes are listed in `CHANGELOG.md` under `Unreleased`.
2. Run quality gates locally:

   ```bash
   bundle exec rspec
   bundle exec rubocop
   ```

3. Verify override drift checks as needed against the target host app:

   ```bash
   ruby bin/check-override-drift /path/to/mastodon
   ```

4. Bump the gem version in `lib/newsmast_mastodon/version.rb`.
5. Move relevant `Unreleased` entries to the release section in `CHANGELOG.md`.
6. Tag the release with `v<gem-version>` and push the tag.
7. Verify GitHub Actions release workflow completes and gem is published.
8. Verify release checksums (`pkg/SHA256SUMS`) and build provenance attestation
   were generated by the release workflow.
